import 'package:flutter/material.dart';

class Detailscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter Basic home secren'),
      ),
      // drawer: Mydrawer(),
      body: Center(
        child: Text('homw screen World'),
      ),
    );
  }
}
